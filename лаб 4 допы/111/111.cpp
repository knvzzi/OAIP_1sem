#include<iostream>
using namespace std;

int main()
{
	/*setlocale(LC_CTYPE, "Russian");
	int b = 7;
	float a = 0.5, w, c = 0;
	float v[] = { 5, 2.5, 3, 8, 9, 3 };

	for (int i = 0; i < 6; i++)
	{
		if (v > 0)
			w = a + v[i];
		else if (v <= 0)
			w = b / v[i];
		if (w > c)
			c = w;
	}
	cout << c;*/

		/*setlocale(LC_CTYPE, "Russian");
		float n = 5, x = 6, i = 1;
		float f = 0;
		while (i <= n) 
		{
			f = f + (i / x);
			i++;

			cout << "f = " << f << endl;
		}*/
}